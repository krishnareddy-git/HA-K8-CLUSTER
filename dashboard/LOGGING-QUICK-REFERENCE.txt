╔══════════════════════════════════════════════════════════════════════╗
║          K8s Dashboard - Unified Logging Quick Reference             ║
╚══════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────┐
│ 🚀 START ALL DASHBOARDS                                              │
└──────────────────────────────────────────────────────────────────────┘

  cd /home/student/Sainath/HA-K8-Cluster/dashboard
  ./start-all-dashboards.sh

  Then open in browser:
    • Main Dashboard:      http://localhost:8080/index.html
    • Load Balancer Demo:  http://localhost:8081/load-balancing-demo.html
    • Log Viewer:          http://localhost:8082/log-viewer.html

┌──────────────────────────────────────────────────────────────────────┐
│ ⌨️  KEYBOARD SHORTCUTS (in Log Viewer)                               │
└──────────────────────────────────────────────────────────────────────┘

  Ctrl/Cmd + K    Focus search box
  Ctrl/Cmd + L    Clear all logs
  Space           Pause/Resume logging
  Escape          Clear all filters

┌──────────────────────────────────────────────────────────────────────┐
│ 🎨 LOG LEVELS & COLORS                                               │
└──────────────────────────────────────────────────────────────────────┘

  🔵 Info       General information (blue)
  ✅ Success    Successful operations (green)
  ⚠️  Warning   Warnings or alerts (orange)
  ❌ Error      Errors and failures (red)

┌──────────────────────────────────────────────────────────────────────┐
│ 📊 LOG SOURCES                                                       │
└──────────────────────────────────────────────────────────────────────┘

  📈 dashboard      Main K8s dashboard events
  ⚖️  loadbalancer   Load balancer demo events
  ⚙️  system         System & log viewer events

┌──────────────────────────────────────────────────────────────────────┐
│ 🔍 FILTERING LOGS                                                    │
└──────────────────────────────────────────────────────────────────────┘

  Source Filter:    All | Dashboard | Load Balancer | System
  Level Filter:     All | Info | Success | Warning | Error
  Search Box:       Type any text to filter logs

  Quick Actions:
    • Show Errors Only      Filter to errors instantly
    • LB Logs Only          Show only load balancer logs
    • Clear Filters         Reset all filters

┌──────────────────────────────────────────────────────────────────────┐
│ 🛠️  CONTROL BUTTONS                                                  │
└──────────────────────────────────────────────────────────────────────┘

  ⬇️   Auto-scroll    Keep view at bottom as new logs arrive
  ⏸️   Pause          Pause logging (data still collected)
  🗑️   Clear          Delete all logs (permanent!)
  💾  Export         Download logs as JSON
  🔄  Refresh        Reload log viewer

┌──────────────────────────────────────────────────────────────────────┐
│ 💻 PROGRAMMATIC USAGE                                                │
└──────────────────────────────────────────────────────────────────────┘

  In Browser Console:

  // Log messages
  logInfo('message', 'source')
  logSuccess('message', 'source')
  logWarning('message', 'source')
  logError('message', 'source')

  // Advanced logging
  unifiedLogger.log('info', 'message', 'source', {metadata})

  // Get statistics
  unifiedLogger.getStats()

  // Export logs
  unifiedLogger.exportLogs()

  // Clear logs
  unifiedLogger.clearLogs()

┌──────────────────────────────────────────────────────────────────────┐
│ 🎯 BEST PRACTICES                                                    │
└──────────────────────────────────────────────────────────────────────┘

  ✓ Open Log Viewer in separate window/monitor
  ✓ Position side-by-side with dashboards for real-time viewing
  ✓ Use filters to focus on specific components or issues
  ✓ Export logs before clearing for record keeping
  ✓ Use error filter during debugging sessions
  ✓ Keep auto-scroll on for monitoring, off for reviewing

┌──────────────────────────────────────────────────────────────────────┐
│ 🧪 TEST SCENARIOS                                                    │
└──────────────────────────────────────────────────────────────────────┘

  Load Balancer Demo:
    1. Send single request       → Watch routing logs
    2. Send burst requests       → See multiple pod logs
    3. Toggle auto-requests      → Monitor continuous traffic
    4. Disable a pod             → See failover messages
    5. Change algorithm          → Log algorithm switch

  Main Dashboard:
    1. Navigate pages            → Track page loads
    2. Refresh data              → See fetch operations
    3. View different sections   → Monitor data retrieval

┌──────────────────────────────────────────────────────────────────────┐
│ 🛑 STOP DASHBOARDS                                                   │
└──────────────────────────────────────────────────────────────────────┘

  ./stop-all-dashboards.sh

┌──────────────────────────────────────────────────────────────────────┐
│ 🐛 TROUBLESHOOTING                                                   │
└──────────────────────────────────────────────────────────────────────┘

  Logs not showing?
    → Check browser console for errors
    → Verify unified-logger.js is loaded: console.log(window.unifiedLogger)
    → Clear localStorage and refresh

  Too many logs?
    → Use filters to narrow down
    → Clear old logs
    → System auto-limits to 500 most recent

  Storage full?
    → System auto-manages storage
    → Manually clear: unifiedLogger.clearLogs()

┌──────────────────────────────────────────────────────────────────────┐
│ 📦 FILES                                                             │
└──────────────────────────────────────────────────────────────────────┘

  unified-logger.js            Core logging engine
  log-viewer.html              Log viewer UI
  log-viewer.js                Log viewer logic
  log-viewer-styles.css        Styling
  start-all-dashboards.sh      Launch script
  stop-all-dashboards.sh       Stop script

┌──────────────────────────────────────────────────────────────────────┐
│ 💡 PRO TIPS                                                          │
└──────────────────────────────────────────────────────────────────────┘

  • Logs persist across browser sessions (saved in localStorage)
  • Click metadata icon (ℹ️) on logs to see additional details
  • Use Pause when inspecting specific logs without new ones appearing
  • Statistics panel shows real-time counts and distributions
  • Export logs before demos/presentations for backup

╔══════════════════════════════════════════════════════════════════════╗
║  For detailed documentation, see: UNIFIED-LOGGING-README.md          ║
╚══════════════════════════════════════════════════════════════════════╝
